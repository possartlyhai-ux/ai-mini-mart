# Dependencies (project is intentionally dependency-free, but just in case)
node_modules/

# Logs
*.log
npm-debug.log*

# OS / editor cruft
.DS_Store
Thumbs.db
*.swp

# Local Claude Code settings (personal, machine-specific)
.claude/settings.local.json
.claude/launch.json

# Firebase deploy cache (local artifact, not source)
.firebase/

# Backend lives/deploys separately — keep it out of this public storefront repo
# (it contains its own .env, dev.db, uploads). Remove this line if you decide
# to track the backend here instead.
backend/
