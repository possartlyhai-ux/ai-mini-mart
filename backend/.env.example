# Copy to .env to override defaults (all optional for local dev).
# SQLite file location (Prisma reads DATABASE_URL).
DATABASE_URL="file:./dev.db"
# Secret used to sign the auth JWT cookie. Change in any real deployment.
JWT_SECRET="dev-secret-change-me"
# Port the admin app + API listen on.
PORT=3000
